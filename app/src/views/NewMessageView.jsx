import React from "react";
import styled from "styled-components";
import connect from "react-redux";
const StyledNewMessageView = styled.div``;
export default function NewMessageView(props) {
  return <StyledNewMessageView />;
}
